const superHeros: string[] = [];
// const heroPower: number[] = [];
const heroPower: Array <number> = []; //generic

type User = {
    name: string
    isActive: boolean
}

const allUsers: User[] = [];
allUsers.push({ name: "", isActive: true});

//2d array 
const twoDimArray: number[][] = [
    [1],[2],[3],
    [4]
]


superHeros.push("spiderman");
console.log(superHeros);

export {}